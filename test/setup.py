# setup.py, config file for distutils

from distutils.core import setup

import py2exe

## setup(name="Flash",
##       scripts=["test_flash.py"])

##setup(name="Dialog",
##      scripts=["test_dialog.py"])

setup(name="Snakepad",
      scripts=["test_scintilla.py"])

      
